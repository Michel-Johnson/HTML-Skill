interface:
  display_name: "HTML Reply"
  short_description: "按明确要求生成可刷新、带历史记录的 HTML 回复"
  default_prompt: "Use $html-reply to deliver this response as a polished HTML page."
policy:
  allow_implicit_invocation: false
